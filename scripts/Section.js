export default class Section{
    constructor({items, renderer}, cardElement){
        this._items = items;
        this._renderer = renderer;
        this._cardElement = document.querySelector(cardElement);
    }

    renderer(){

    }

    addItem(){

    }
}